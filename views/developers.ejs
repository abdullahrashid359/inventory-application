<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Developers - GameVault</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <%- include('partials/navbar') %>

    <main class="main-content">
        <div class="page-header">
            <h1 class="page-title">Developers</h1>
            <a class="button" href="/developers/new">Add Developer</a>
        </div>

        <section class="category-list">
            <% developers.forEach(developer => { %>
                <article class="category-card">
                    <h2 class="category-card-title">
                        <a href="/developers/<%= developer.id %>">
                            <%= developer.name %>
                        </a>
                    </h2>

                    <div class="category-card-actions">
                        <a
                            class="button button-small"
                            href="/developers/<%= developer.id %>"
                        >
                            View
                        </a>

                        <a
                            class="button button-small button-secondary"
                            href="/developers/<%= developer.id %>/edit"
                        >
                            Edit
                        </a>

                        <form
                            action="/developers/<%= developer.id %>/delete"
                            method="POST"
                            onsubmit="return confirm('Are you sure you want to delete this developer?');"
                        >
                            <button
                                class="button button-small button-danger"
                                type="submit"
                            >
                                Delete
                            </button>
                        </form>
                    </div>
                </article>
            <% }) %>
        </section>
    </main>
</body>
</html>